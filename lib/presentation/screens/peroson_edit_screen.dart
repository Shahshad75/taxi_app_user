import 'package:flutter/material.dart';

class PersonEditingScreen extends StatelessWidget {
  const PersonEditingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: const SafeArea(
            child: Center(
          child: Column(
            children: [
              
            ],
          ),
        )));
  }
}
